[project]
name = "dlt-pipelines"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "dlt[bigquery,cli]>=1.21.0",
    "prefect>=3.6.13",
    "prefect-cloud>=0.1.9",
]
